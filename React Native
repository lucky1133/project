If You Want To Make Dark Mode

import { useColorScheme } from "react-native";

const colorScheme = useColorScheme();
const isDark = colorScheme === 'dark';

/*****************/
backgroundColor:isDark ? '#15263A' : '#fff',
